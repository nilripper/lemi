[package]
name = "lemi"
version = "0.1.0"
edition = "2021"

[features]
default = ["std"]
std = []

[dependencies]
libm = { version = "0.2", default-features = false }

[dev-dependencies]
serde_json = "1"
